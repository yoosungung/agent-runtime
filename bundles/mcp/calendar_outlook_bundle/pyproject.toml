[project]
name = "calendar-outlook-bundle"
version = "0.1.0"
description = "Production MCP Microsoft 365 Calendar server bundle"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mcp>=1.27",
    "httpx>=0.27",
    "msal>=1.31",
    "runtime-common",
]

[tool.uv.sources]
runtime-common = { workspace = true }
