[project]
name = "search-bundle"
version = "0.1.0"
description = "Production MCP search server bundle (Naver search + URL fetch)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mcp>=1.27",
    "httpx>=0.27",
    "runtime-common",
]

[tool.uv.sources]
runtime-common = { workspace = true }
