# Dependencies / build
node_modules/
dist/
dist-ssr/
.vite/

# TypeScript incremental build cache (tsc -b)
*.tsbuildinfo

# Env (keep .env.example tracked)
.env
.env.*
!.env.example
*.local

# Test outputs
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/
